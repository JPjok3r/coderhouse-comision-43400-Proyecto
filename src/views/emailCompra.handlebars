<section class="vh-100">
  <div class="container py-5 h-100">
    <div class="row d-flex align-items-center justify-content-center h-100">

      <div class="col-md-7 col-lg-5 col-xl-5 offset-xl-1">
        <p class="lead">
          Gracias por su compra, {{data.purcchaser}}
        </p>
        <h3>
            Datos de compra
        </h3>
        <p>id compra: {{data.code}}</p>
        <p>Monto total: {{data.amout}}</p>
        <p>Fecha de compra: {{data.purchase_datetime}}</p>
      </div>
    </div>
  </div>
</section>